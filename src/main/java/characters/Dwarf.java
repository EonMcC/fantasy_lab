package characters;

import components.WeaponType;

public class Dwarf extends Fighter {

    public Dwarf(String name, int maxHP, String catchPhrase, WeaponType weapon) {
        super(name, maxHP, catchPhrase, weapon);
    }
}
